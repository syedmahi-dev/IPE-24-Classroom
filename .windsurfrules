# UI/UX Design Rule
- **CRITICAL**: Whenever you are asked to implement, design, change, or fix anything related to UI/UX, frontend interfaces, layouts, or visual styles, you MUST use the "UI/UX Pro Max" skill framework.
- **Workflow**:
  1. Review the `d:\IPE-24 Classroom\.agent\skills\ui-ux-pro-max\SKILL.md` instructions before writing UI code.
  2. Follow its Pre-Delivery Checklist strictly. Use Lucide SVG icons, smooth transitions (150-300ms), ensure light/dark mode contrast, correct layout spacings, and glassmorphism defaults (`.glass`, `.glass-dark`).
  3. If Python is available, run the `search.py` script as outlined in the skill to generate a design system first. If it is not available, you MUST still act according to the high-level premium heuristics.
  4. Never output plain, flat, default layouts. Your design output must always be premium, interactive, well-animated, and highly responsive.

# AI Safety & DB Policy (MANDATORY)
- **Backup Before Change**: BEFORE modifying `schema.prisma` or core configs, you MUST create a `.bak` copy of the file.
- **Data Preservation**: NEVER run `db push --accept-data-loss` without confirming that critical data (like routine) is backed up or reproducible via seeds.
- **Feature Isolation**: Deal ONLY with the specific database models relevant to the current feature. Do not touch unrelated tables.
- **Documentation**: Any update to the database or core features MUST be documented in `AI_SAFETY_POLICY.md` or a new feature-specific doc.
- **Read Rules**: You MUST read `AI_SAFETY_POLICY.md` at the start of every session.
